name: '$(Build.SourceBranchName)$(System.PullRequest.SourceBranch)$(Rev:.r)'

trigger:
  - main
  - feature/*
  - release/*

pr:
  - main
  - releases/*

parameters:
  - name: prDeployment
    type: boolean
    default: true
  - name: branchDeployment
    type: boolean
    default: true
  - name: sndDeployment
    type: boolean
    default: true

resources:
  repositories:
    - repository: templates
      endpoint: DEFRA
      type: github
      name: defra/fcp-pipeline-common
      # ref: branch-name

extends:
  template: fcp-common-pipeline.yaml@templates
  parameters:
    appFrameworkType: nodejs
    prDeployment: ${{ parameters.prDeployment }}
    branchDeployment: ${{ parameters.branchDeployment }}
    sndDeployment: ${{ parameters.sndDeployment }}
    variableGroupName: ffc-doc-statement-generator-env-var
    pr_params:
      endpointEnabled: ${{ eq(variables['pr_endpointEnabled'], 'true') }}
    snd2_params:
      endpointEnabled: ${{ eq(variables['snd2_endpointEnabled'], 'true') }}
    dev_params:
      endpointEnabled: ${{ eq(variables['dev_endpointEnabled'], 'true') }}
    test_params:
      endpointEnabled: ${{ eq(variables['test_endpointEnabled'], 'true') }}
    pre_params:
      endpointEnabled: ${{ eq(variables['pre_endpointEnabled'], 'true') }}
    prd_params:
      endpointEnabled: ${{ eq(variables['prd_endpointEnabled'], 'true') }}
